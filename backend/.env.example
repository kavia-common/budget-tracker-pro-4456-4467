# Backend Environment Variables

# Server
PORT=3000
# HOST is optional; defaults to 0.0.0.0 if not set
# HOST=0.0.0.0

# Database
# Full PostgreSQL connection string, e.g.:
# postgresql://user:password@host:5001/dbname
POSTGRES_URL=postgresql://USER:PASSWORD@HOST:5001/DBNAME

# Auth
JWT_SECRET=replace-with-a-secure-random-string
# Token expiry in a format supported by jsonwebtoken (e.g., '1h', '7d')
TOKEN_EXPIRY=1h
